# Keep the build context small. Generated code (api/gen) is intentionally
# excluded — the image's `proto` stage regenerates it via buf generate.
.git
.github
.claude
docs
bin
deploy/data
api/gen
*.log
*.md
