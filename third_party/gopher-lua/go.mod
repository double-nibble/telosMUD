module github.com/yuin/gopher-lua

// TELOSMUD FORK of github.com/yuin/gopher-lua v1.1.1 (see FORK.md).
// The upstream cmd/glua REPL (its only user of github.com/chzyer/readline) was
// removed from this in-tree copy, so the readline dependency is dropped here.

go 1.17
